import Inferno from 'inferno'

/* function button (dispatch, {texts, children}) {
  return (
    <div>
      <h1>{texts.text}</h1>
    </div>
  )
} */

function addnumbers (a, b) {
  return a + b
}

export { addnumbers }
